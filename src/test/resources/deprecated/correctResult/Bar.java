package deprecated;

import java.util.List;

public class Bar {
	private boolean test = new Foo("a").test5();
	private boolean unused;

	public static foo() {
		List.of("a").stream().forEach((v) -> new Foo(v).test4());
	}
}
