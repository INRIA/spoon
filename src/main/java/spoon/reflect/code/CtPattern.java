package spoon.reflect.code;

// TODO reconsider, could be removed but could also be handy in future (but could also be reintroduced then)

/**
 * This interface represents different types of patterns available
 * in the Java language.
 */
public interface CtPattern extends CtExpression<Void> {

}
