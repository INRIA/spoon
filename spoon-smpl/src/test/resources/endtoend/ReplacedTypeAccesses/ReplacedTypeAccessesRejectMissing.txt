[name]
ReplacedTypeAccessesRejectMissing

[contract]
the sub-expression "LARGER" in the patch should NOT match the expression "WebSettings.TextSize.LARGER" in the code, no transformation should be applied (missing information version). validate-e2e: purposefully-invalid

[patch]
@@ @@
- setTextSize(LARGER);

[input]
class A {
  /* skip */ public void setTextSize(Object x) {}

  public void m1() {
    setTextSize(WebSettings.TextSize.LARGER);
  }
}

[expected]
class A {
  /* skip */ public void setTextSize(Object x) {}

  public void m1() {
    setTextSize(WebSettings.TextSize.LARGER);
  }
}
