/**
 * This package defines the core runner with related interfaces. The runner invokes all architectureTests.
 * SpoonArchitecturalChecker is the main entry point for the runner api.
 * If a user wants to have a different method selection algorithm, {@code IRunner} defines an interface for them.
 */
package spoon.architecture.runner;
